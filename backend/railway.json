{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn vin_project.wsgi --workers 4 --bind 0.0.0.0:$PORT --timeout 120",
    "healthcheckPath": "/api/health/",
    "restartPolicyType": "ON_FAILURE"
  }
}
